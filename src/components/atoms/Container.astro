---
/** Contenedor centrado de ancho máximo (el ".wrap" del mockup). */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['mx-auto w-full max-w-[1180px] px-8 max-sm:px-5', className]}>
  <slot />
</div>
